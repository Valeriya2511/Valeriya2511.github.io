export interface iLineItem {
  name: string;
  priceCent: number;
  currencyCode: string;
  productId: string;
  productSlug: string;
  quantity: number;
  totalItemPriceCent: number;
  images: string;
}
